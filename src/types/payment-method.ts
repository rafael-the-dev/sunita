

export type PaymentMethodType = {
    receivedAmount: number;
    id: string | number;
    amount: number;
}

export type PaymentMethodListItemType = {
    label: string;
    value: string | number
}