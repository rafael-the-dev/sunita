
export enum COLLAPSE {
    CHART_TYPE = "chart",
    X_AXIS = "x-axis",
    Y_AXIS = "y-axis"
}

export enum CHART_TYPE {
    BAR = "bar",
    LINE = "line"
}

export enum TABS {
    CHART = "chart",
    SCHEDULER = "scheduler"
}

export enum X_AXIS {
    DAY = "day",
    WEEK = "week",
    MONTH = "month",
    YEAR = "year"
}