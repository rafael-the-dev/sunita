
export enum COLLAPSE {
    X_AXIS = "x-axis",
    Y_AXIS = "y-axis"
}

export enum TABS {
    CHART = "chart",
    SCHEDULER = "scheduler"
}

export enum X_AXIS {
    DAY = "day",
    WEEK = "week",
    MONTH = "month",
    YEAR = "year"
}